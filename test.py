import robot
example_a = "0,0,NORTH MOVE REPORT"
example_b = "0,0,NORTH LEFT REPORT"
example_c = "1,2,EAST MOVE MOVE LEFT MOVE REPORT"

robot.compute(example_a)
robot.compute(example_b)
robot.compute(example_c)
